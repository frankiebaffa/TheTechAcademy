Open 'build_db_baffaLibrary.sql' in SQL Server and execute
	(this will create the database, create the tables,
	and populate the tables)

Open 'procQuestions.sq.' in SQL Server and execute
	(this will create the procedures to query each test question
	and execute them)
